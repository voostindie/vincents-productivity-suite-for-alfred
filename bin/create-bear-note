#!/usr/bin/env ruby
#
# Creates a new note in Bear and opens it for editing
#
# Requires one argument: the title of the note
#  (if there are multiple arguments, these are joined together)
# Output: nothing

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'vps'

VPS::Alfred.action do
  note = VPS::Bear::Note.new(ARGV.join(' '))
  note.create_file
end
