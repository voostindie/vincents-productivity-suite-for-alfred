#!/usr/bin/env ruby
#
# Lists all available areas of responsibility

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'vps'

VPS::OutputFormatter::Alfred.format do
  VPS::Area.list
end
